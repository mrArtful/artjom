/* =========================
   TASK 1: Dynamic List
   HTML:
     <input id="itemInput">
     <button id="addBtn">Add</button>
     <ul id="list"></ul>

   - Button adds new <li> with the input value
   - Empty input → do nothing
   - Clear input after adding
========================= */

// ✅ Write your code here:



/* =========================
   TASK 2: Delete Item (extends Task 5)
   Extend Task 5 so that each <li> contains:
   - the item text
   - a ❌ delete button

   Clicking ❌ removes ONLY that item.
   (Hint: create button element, add click listener, remove li)
========================= */

// ✅ Write your code here:



/* =========================
   TASK 3: Highlight Active Button
   HTML:
     <div class="buttons">
       <button>HTML</button>
       <button>CSS</button>
       <button>JS</button>
     </div>

   - Clicking a button:
     - adds class "active" to clicked button
     - removes "active" from all other buttons
========================= */

// ✅ Write your code here:



/* =========================
   TASK 4: Theme Switcher (medium)
   HTML:
     <button id="themeBtn">Dark mode</button>

   - Toggle class on <body> (e.g. "dark")
   - Dark mode styles (in CSS):
     body.dark { background: #222; color: #fff; }
   - Button text must switch:
     - "Dark mode" / "Light mode"
========================= */

// ✅ Write your code here:



/* =========================
   TASK 5: DOM Validation (real-life)
   HTML:
     <input id="email">
     <button id="submitBtn">Submit</button>
     <p id="error"></p>

   - Email must include "@"
   - If invalid:
     - show error message in <p>
   - If valid:
     - clear error
     - alert("Success")
========================= */

// ✅ Write your code here:



/* =========================
   BONUS (optional): Global Button Click Counter
   Goal:
   - Count how many times ANY button on the page was clicked
   - Show total count in a <p> (create it in HTML or create via JS)

   Hints:
   - querySelectorAll("button")
   - loop and addEventListener
========================= */

// ✅ Write your code here: