// Задача 1. Информация о пользователе
// 1. Создайте объект `user` со свойствами:
//    - firstName (строка)
//    - lastName (строка)
//    - age (число)
//    - email (строка)
// 2. С помощью шаблонной строки выведите:
//    “Пользователь <firstName> <lastName> (возраст: <age>) доступен по адресу <email>.”
// 3. Преобразуйте весь текст результата в верхний регистр.




// Задача 2. Расширение объекта через spread
// const defaults = {
//   theme: "light",
//   notifications: true,
//   location: "UTC"
// };
// 1. Создайте новый объект `userSettings`, который:
//    - содержит все поля из `defaults`
//    - переопределяет `theme` на "dark"
//    - добавляет поле `fontSize: 14`




// Задача 3. Объединение и клонирование
// const objA = { a: 1, b: 2 };
// const objB = { b: 3, c: 4 };
// 1. Объедините `objA` и `objB` в объект `merged`, чтобы значение `b` было из `objB`.
// 2. Сделайте копию `merged` в объект `clone`.
// 3. Измените в `clone` свойство `a` на 99.
// 4. Проверьте, что в `merged.a` осталось исходное значение.




// Задача 4. Объединение настроек с дефолтами
// const configDefaults = { volume: 50, brightness: 70 };
// const userConfig = { brightness: 90, contrast: 40 };
// Создайте новый объект `config`, который:
//  - включает все поля из `configDefaults`,
//  - затем перекрывает их полями из `userConfig`,
//  - затем добавляет поле `mode: "auto"`.




// Задача 5. Вычисляемые имена свойств
// 1. Создайте переменную `key = "score"`.
// 2. Создайте объект `result` так, чтобы в нём было поле с именем из переменной `key` и значением 100.
// 3. Выведите `result.score`.



